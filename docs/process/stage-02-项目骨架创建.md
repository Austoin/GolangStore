# 阶段 02：项目骨架创建

## 时间
- 2026-03-24

## 阶段目标
- 建立项目骨架、模块名、基础工程文件与目录结构。
- 严格按先基础、后服务、再业务的顺序推进。

## 固定模块信息
- Go Module：`github.com/austoin/GolangStore`
- Monorepo 根目录保留统一模块，不在一期拆分多个独立 Go module。

## 固定执行顺序
1. 建立仓库基础目录与 Go 模块。
2. 建立公共工程文件与本地运行配置。
3. 建立服务入口与健康检查骨架。
4. 建立数据库初始化脚本。
5. 实现商品查询。
6. 实现购物车 CRUD。
7. 实现普通下单与订单查询。
8. 接入 Redis 库存缓存。
9. 接入 Lua 秒杀扣减与回补。
10. 实现 MCP 网关与两个核心 Skills。
11. 补齐 Dockerfile 与 Compose 联调。
12. 最后整理 README、接口说明与运行文档。

## 当前批次策略
- 批次 1：文档补充 + Go 模块初始化。
- 批次 2：基础工程文件 + 根目录结构创建。
- 批次 3：服务空壳入口 + 健康检查。

## 当前约束
- 当前阶段不写业务接口实现。
- 当前阶段不引入数据库表定义之外的业务逻辑。
- 当前阶段不直接创建超过 3 类职责的复杂实现文件。

## 本阶段已完成
- 已创建根目录：`cmd/`、`internal/`、`pkg/`、`configs/`、`deploy/`、`scripts/`、`test/`
- 已初始化工程文件：`go.mod`、`go.work`、`Makefile`、`.env.example`、`docker-compose.yml`
- 已补充 `README.md` 作为项目总览入口

## 当前验证结果
- 根目录骨架已存在，可继续进入服务入口创建阶段。
- 当前 Compose 仅包含 MySQL 与 Redis，占位合理，未提前引入额外基础设施。

## 下一步
- 创建各服务入口目录与最小健康检查程序。
- 保持服务只具备启动与健康探测能力，不进入业务实现。

## 批次 5A：首批服务入口骨架

### 本批次目标
- 先创建首批 3 个核心服务入口，验证“多服务可独立启动”的基本模式。
- 继续避免过早引入业务实现与共享抽象。

### 本批次已完成
- 已创建目录：
  - `cmd/gateway/`
  - `cmd/product-service/`
  - `cmd/order-service/`
- 已创建最小入口文件：
  - `cmd/gateway/main.go`
  - `cmd/product-service/main.go`
  - `cmd/order-service/main.go`

### 当前实现约束
- 当前仅提供 `/health` 健康检查接口。
- 当前直接使用 Gin 启动服务，不引入公共启动器。
- 当前不抽离公共 router 初始化逻辑，避免在骨架阶段提前设计共享框架。

### 当前端口分配
- `gateway`：`8080`
- `product-service`：`8081`
- `order-service`：`8082`

### 当前状态
- 已形成首批可独立启动的服务骨架模式。
- 下一批将按同样结构补齐剩余服务。

### 本批次验证与修正
- 创建服务入口后，`gin` 依赖缺失，触发模块解析错误。
- 已执行 `go mod tidy` 补齐依赖。
- 当前 `go.mod` 已纳入 `github.com/gin-gonic/gin`。

### 当前注意事项
- `go mod tidy` 将 `go.mod` 中的 Go 版本更新为本机工具链版本 `1.25.0`。
- 后续如需与目标环境统一，可在基础设施阶段再统一锁定 Go 版本策略。

## 批次 5B：剩余服务入口骨架

### 本批次目标
- 补齐剩余 3 个服务入口，完成一期全部服务的最小启动骨架。

### 本批次已完成
- 已创建目录：
  - `cmd/cart-service/`
  - `cmd/user-service/`
  - `cmd/mcp-gateway/`
- 已创建最小入口文件：
  - `cmd/cart-service/main.go`
  - `cmd/user-service/main.go`
  - `cmd/mcp-gateway/main.go`

### 当前端口分配补充
- `cart-service`：`8083`
- `user-service`：`8084`
- `mcp-gateway`：`8085`

### 当前状态
- 一期规划中的全部服务均已具备独立启动入口。
- 服务骨架阶段下一步应转入统一编译验证与目录收口检查。

## 批次 6：服务骨架编译级验证

### 本批次目标
- 验证全部服务骨架至少在编译层面成立。
- 修正初始化阶段留下的 Go workspace 版本不一致问题。

### 发现的问题
- 执行 `go test ./...` 时，`go.work` 与 `go.mod` 的 Go 版本不一致：
  - `go.mod`：`1.25.0`
  - `go.work`：`1.23.0`

### 已完成修正
- 已将 `go.work` 同步更新为 `1.25.0`。
- 已重新执行 `go test ./...`。

### 当前验证结果
- 6 个服务入口均已通过编译级验证。
- 当前输出为 `no test files`，符合骨架阶段预期。

### 当前阶段结论
- 项目已完成根目录骨架、基础工程文件和全部服务入口创建。
- 当前状态适合进入下一阶段：公共基础设施包与数据库初始化脚本。
