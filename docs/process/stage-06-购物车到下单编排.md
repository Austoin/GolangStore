# 阶段 06：购物车到下单编排

## 时间
- 2026-03-24

## 阶段目标
- 打通从购物车已勾选项到订单创建的最小业务闭环。
- 先完成应用层编排，再考虑数据库持久化与库存扣减。

## 当前计划顺序
1. 提取已勾选购物车项
2. 定义购物车到下单的编排输入
3. 实现应用服务编排
4. 提供最小 HTTP 创建入口
5. 更新文档并做全量验证

## 当前约束
- 当前阶段不接入 MySQL。
- 当前阶段不接入 Redis 扣库存。
- 当前阶段不实现支付成功、关闭订单等后续状态流转。

## 当前状态
- 阶段刚开始，尚未创建购物车到下单编排代码。

## 当前批次进展

### 已完成内容
- 已扩展 `internal/cart/service.go`：
  - `ListCheckedItems(...)`
- 已扩展 `internal/order/service.go`：
  - `CreateOrderFromCheckedItems(...)`
- 已扩展 `internal/order/handler.go`：
  - `CreateFromCheckedCartItems(ctx)`

### 当前行为范围
- 可按用户过滤出已勾选购物车项。
- 可基于已勾选购物车项创建订单。
- 可过滤空购物车下单请求并返回错误。
- 订单状态初始化为 `pending`。

### 当前限制
- 当前编排仍通过请求体直接传入购物车项，不是从持久化购物车仓储读取。
- 当前未接入库存校验与扣减。
- 当前未接入 MySQL 持久化。

### 当前验证记录
- 已执行：`go test ./internal/cart`
- 已执行：`go test ./internal/order`
- 当前已验证通过：
  - 已勾选购物车项提取
  - 购物车到下单金额汇总
  - 空购物车失败
  - 编排 handler 创建订单
